using System;
using Xamarin.Forms;
using Xamarin.Forms.CommonCore;

namespace somenamespace
{
    public class SomePage : BoundPage<SomeViewModel>
    {
        public SomePage()
        {
            this.Title = "Some Page";

            var lbl = new Label() { Text = "I am a label" };

            Content = new StackLayout()
            {
                Padding = 20,
                Children = { lbl }
            };
        }
    }
}